datasource db {
  provider = "postgresql"
  url = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Waypoint {
  id         Int     @id @default(autoincrement())
  name       String
  x          Int
  y          Int
  gameCoords String
  category   String
}
